# et-hotel-ai-showcase — runtime + test dependencies (pinned for reproducibility).
# Install into the project venv:  make install   (or: pip install -r requirements.txt)
#
# The default MODEL_BACKEND=mock needs NO network at runtime and NO API keys.
# langchain-openai / langfuse are only exercised for the ollama/openrouter backends.
# Versions verified green on the CI matrix (Python 3.11 and 3.12).
langgraph==1.2.9
langchain-core==1.5.0
langchain-openai==1.4.0
langfuse==4.14.1
pydantic==2.13.4
pydantic-settings==2.14.2
pandas==3.0.3
python-dotenv==1.2.2
temporalio==1.30.0
curl_cffi==0.15.0
httpx==0.28.1
click==8.4.2
# Interactive showcase dashboard (Streamlit Community Cloud entrypoint:
# streamlit_app.py). Runs the app fully offline on MODEL_BACKEND=mock — no keys,
# no network. The architecture diagram loads mermaid.js client-side (browser CDN),
# so no extra Python component dependency is required.
streamlit==1.60.0
pytest==9.1.1
pytest-asyncio==1.4.0
